import { FileDropUploadDirective } from './file-drop-upload.directive';

describe('FileDropUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDropUploadDirective();
    expect(directive).toBeTruthy();
  });
});
